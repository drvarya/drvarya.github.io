{
  "name": "dhruv-arya-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site",
    "deploy": "npm run build && gh-pages -d _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gh-pages": "^6.3.0"
  }
}
